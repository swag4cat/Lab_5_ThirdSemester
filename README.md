# Генератор пещер - Клеточные автоматы

Графическая система генерации пещер с использованием алгоритмов клеточных автоматов. Этот проект реализует генерацию пещер в рамках Лабораторной работы №5 по курсу программирования.

## 🎯 Возможности

- **Генерация пещер клеточными автоматами** - Формирование пещер в реальном времени с использованием математических правил
- **Интерактивная графика** - Визуализация на основе SFML с живыми обновлениями
- **Настраиваемые параметры** - Регулировка пределов рождения/смерти и начальных условий

## 🏗️ Структура проекта

```text
Lab5/
├── main.cpp   # Основной исходный код приложения
├── Makefile   # Конфигурация сборки
├── Doxyfile   # Конфигурация документации
└── README.md  # Документация проекта
```

## 📋 Требования

### Зависимости

- **Компилятор C++** (g++ 7.0+ или эквивалент)
- **SFML 2.5+** - Простая и быстрая мультимедийная библиотека
- **Doxygen** (опционально) - Для генерации документации
- **Graphviz** (опционально) - Для UML-диаграмм

### Установка на Ubuntu/Debian
```bash
sudo apt-get update
sudo apt-get install libsfml-dev doxygen graphviz
```

## 🔧 Опции сборки

```bash
make              # Сборка исполняемого файла
make run          # Сборка и запуск приложения
make doc          # Генерация документации (требует Doxygen)
make clean        # Удаление собранных файлов
make clean-doc    # Удаление документации
make distclean    # Удаление всего
```

## 🧪 Детали алгоритма

Генерация пещеры следует этим правилам на каждой итерации:

- **Правило рождения:** Мертвые клетки с более чем birthLimit живыми соседями оживают

- **Правило смерти:** Живые клетки с менее чем deathLimit живыми соседями умирают

- **Стазис:** Клетки с достаточным количеством соседей остаются неизменными
